const myArray = [];
// Llenar el arreglo
for (let index = 0; index <= 100; index++) {
    myArray.push(index)
}
console.log(myArray); // debe imprime un array con los numeros de 1 al 100

// Parte 2, Obtener la suma de todos los valores de el array.
// Utilizando un for / while, vamos a recorrer el arreglo myArray
// para ir sumando los valores que contiene el arreglo

let suma = 0;
// Recorrer los elementos que tiene myArray
// ir sumando en la variable suma el valor de cada elemento

// suma = suma + <CUALQUIER_NUMERO>

console.log(suma); // 5050